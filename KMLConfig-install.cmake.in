set(KML_INCLUDE_DIRS
  "${CMAKE_INSTALL_PREFIX}/include";
  "${EXPAT_INCLUDE_DIRS}";
  "${Boost_INCLUDE_DIR}")

set(KML_LIBRARY_DIR  "${CMAKE_INSTALL_PREFIX}/lib")
find_library(KML_LIBRARY kml PATHS "${CMAKE_INSTALL_PREFIX}/lib" NO_DEFAULT_PATH)
