set(KML_INCLUDE_DIRS
  "${CMAKE_INSTALL_PREFIX}/include";
  "${EXPAT_INCLUDE_DIRS}";
  "${Boost_INCLUDE_DIR}")

set(KML_LIBRARY_DIR  "${libkml_BINARY_DIR}")
if(EXISTS "@KML_TARGETS_FILE@" AND NOT TARGET kml)
  include("@KML_TARGETS_FILE@")
endif()
