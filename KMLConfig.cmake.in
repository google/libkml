set(KML_INCLUDE_DIRS
  "${CMAKE_INSTALL_PREFIX}/include";
  "${EXPAT_INCLUDE_DIRS}";
  "${Boost_INCLUDE_DIR}")

set(KML_LIBRARY_DIR  "${libkml_BINARY_DIR}")
if(EXISTS "${CMAKE_CURRENT_LIST_DIR}/KMLTargets.cmake" AND NOT TARGET kml)
  include("${CMAKE_CURRENT_LIST_DIR}/KMLTargets.cmake")
endif()
